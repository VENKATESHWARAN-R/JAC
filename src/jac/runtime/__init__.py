"""JAC runtime core — Gru, sessions, and the event bus glue.

Phase 0 ships only ``gru.build_gru()``. Session lifecycle and the hooks-driven
event bus arrive in Phase 1 (see docs/architecture.md §9).
"""
